# Installed into a separate venv (/opt/dbt-venv), not Airflow's own — see
# the Dockerfile and ADR-008. Keep in sync with ../../dbt/requirements.txt
# (duplicated rather than shared across the Docker build context boundary).
dbt-core>=1.8
dbt-bigquery>=1.8
