# Python
.venv/
.venv-spark/
venv/
__pycache__/
*.pyc
.pytest_cache/
.ipynb_checkpoints/
*.egg-info/

# Environment / secrets
.env
*-key.json
service-account*.json
*.pem
secrets/*
!secrets/.gitkeep
streamlit_app/.streamlit/secrets.toml

# Raw data (public + synthetic, but never committed in full — see docs/GOVERNANCE.md)
data/raw/*
!data/raw/.gitkeep

# Airflow local runtime
orchestration/airflow/logs/
orchestration/airflow/plugins/
orchestration/airflow/airflow.db

# dbt
dbt/**/target/
dbt/**/dbt_packages/
dbt/**/logs/
dbt/**/.user.yml

# Spark
spark_jobs/**/spark-warehouse/
spark_jobs/**/derby.log
spark_jobs/**/metastore_db/
derby.log
metastore_db/

# Local Delta lakehouse output (Milestone 2 — regenerate with the spark job;
# Milestone 3 ports the same transforms to real Unity Catalog tables)
data/lakehouse/

# OS
.DS_Store

# Claude Code harness runtime state (not project config)
.claude/scheduled_tasks.lock
